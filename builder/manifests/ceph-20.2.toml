name = "ceph-20.2"
collection = "ceph"
version = "20.2.4"
source_type = "git"
repo_url = "https://github.com/ceph/ceph"
# Peeled commit for the official v20.2.4 tag; use the commit rather than the
# mutable Tentacle documentation series URL as the corpus source.
git_ref = "7f793731f1b39eb4f465e960113d2363c311b964"
docs_paths = ["doc"]
sparse_paths = ["doc"]
source_url_template = "{repo_url}/blob/{git_ref}/{path}"
