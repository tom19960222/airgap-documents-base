---
collection: cilium
version: "1.16.7"
title: "cilium-operator-generic_completion_bash"
source_url: https://github.com/cilium/cilium/blob/2ab5f8da5915992a1e548c290105dbc08f4be52d/Documentation/cmdref/cilium-operator-generic_completion_bash.md
fetched_at: 2025-02-13T12:04:31Z
---
<!-- This file was autogenerated via cilium-operator --cmdref, do not edit manually-->

## cilium-operator-generic completion bash

Generate the autocompletion script for bash

### Synopsis

Generate the autocompletion script for the bash shell.

This script depends on the 'bash-completion' package.
If it is not installed already, you can install it via your OS's package manager.

To load completions in your current shell session:

	source <(cilium-operator-generic completion bash)

To load completions for every new session, execute once:

#### Linux:

	cilium-operator-generic completion bash > /etc/bash_completion.d/cilium-operator-generic

#### macOS:

	cilium-operator-generic completion bash > $(brew --prefix)/etc/bash_completion.d/cilium-operator-generic

You will need to start a new shell for this setup to take effect.

```
cilium-operator-generic completion bash
```

### Options

```
  -h, --help              help for bash
      --no-descriptions   disable completion descriptions
```

### SEE ALSO

* [cilium-operator-generic completion](cilium-operator-generic_completion.md)	 - Generate the autocompletion script for the specified shell
